// I AM NOT DONE

export default function main() {
  // fill this array with 1, 2 and 3
  const array = []

  console.log(array)

  assert(
    JSON.stringify(array) === '[1,2,3]',
    'The array should contain 1, 2 and 3'
  )
}
