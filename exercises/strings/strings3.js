// I AM NOT DONE

export default function main() {
  const string = 'hello world !'

  // get the right character from the string above to make the assert pass
  const result = ''

  assert(
    result === '!',
    'the result should be the last character in the string'
  )
}
