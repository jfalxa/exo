// I AM NOT DONE

export default function main() {
  // retrieve the div with the id "root" from the document
  // to know what you are looking for, inspect the html of the web page
  const root = null

  assert(root.tagName === 'DIV', 'the root element should be a div')
  assert(root.id === 'root', `the root element's id should be "root"`)
}
